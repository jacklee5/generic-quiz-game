<!DOCTYPE HTML>
<html>

<head>
    <title><%=setTitle%></title>
    <%- include partials/meta.ejs %>
    <style>
        /* container */
        .container{
            background: white;
            box-shadow: 1px 3px 5px rgba(0,0,0,0.3);
            display: block;
            padding: 16px 32px;
            margin: 32px 0px;
            position: relative;
        }

        /* term */
        .term{
            white-space: nowrap;
        }
        .term input{
            width: calc(50% - 40px);
            white-space: initial;
            margin: 0px 8px;
            border: none;
        }

        /* add term button */
        #add-term{
            cursor: pointer;
        }
        #add-term b{
            text-transform: uppercase;
            text-align: center;
            width: 100%;
            display: block;
        }
    </style>
</head>

<body>
    <header class = "background-primary">
        <h1><%=setTitle%></h1>
        <b>Created by <%=creatorName%></b>
    </header>
    <main>
    <button class = "background-secondary"><i class = "material-icons">play_arrow</i>Play</button>
    <% for(let i = 0; i < terms.length; i++) { %>
        <div class = "term container">
            <input type = "text" name = "terms" placeholder = "term" readonly value = "<%-value=terms[i].term%>">
            <input type = "text" name = "definitions" placeholder = "definition" readonly value = "<%=terms[i].definition%>">
        </div>
    <% } %>
    </main>
</body>

</html>