<!DOCTYPE HTML>
<html>

<head>
    <title>Create New Set</title>
    <%- include partials/meta.ejs %>
    <style>
        /* container */
        .container{
            background: white;
            box-shadow: 1px 3px 5px rgba(0,0,0,0.3);
            display: block;
            padding: 16px 32px;
            margin: 32px 0px;
            position: relative;
        }

        /* term */
        .term{
            white-space: nowrap;
        }
        .term input{
            width: calc(50% - 40px);
            white-space: initial;
            margin: 0px 8px;
        }

        /* add term button */
        #add-term{
            cursor: pointer;
        }
        #add-term b{
            text-transform: uppercase;
            text-align: center;
            width: 100%;
            display: block;
        }
    </style>
</head>

<body>
    <header class = "background-primary">
        <h1>Set Name</h1>
    </header>
    <main>
    <form action = "/create-set" method = "POST">
        <div id = "terms">
            theoretically, there will be terms here
        </div>

        <div id = "add-term" class = "container">
            <b>Add Term</b>
        </div>
    </form>
    </main>
</body>

</html>